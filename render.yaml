services:
  - type: web
    name: year-progress-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web_bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.15